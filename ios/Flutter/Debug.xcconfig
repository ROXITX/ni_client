// Ensure CocoaPods base config is applied for Debug builds (conditional to avoid errors before pods install)
#include? "Pods/Target Support Files/Pods-Runner/Pods-Runner.debug.xcconfig"
#include "Generated.xcconfig"
