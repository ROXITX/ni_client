// Ensure CocoaPods base config is applied for Release builds
// Use conditional includes to avoid errors before pods are installed
#include? "Pods/Target Support Files/Pods-Runner/Pods-Runner.release.xcconfig"
// Also include profile as a fallback per CocoaPods warning (harmless if absent)
#include? "Pods/Target Support Files/Pods-Runner/Pods-Runner.profile.xcconfig"
#include "Generated.xcconfig"
